# DA3 Small Core ML Camera-Token Export

The supported conversion and validation workflow is documented in
[`tools/coreml/README.md`](tools/coreml/README.md).

Run the complete build from the repository root:

```bash
tools/coreml/build_da3_small.sh
```
